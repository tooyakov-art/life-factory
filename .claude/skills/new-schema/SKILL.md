---
name: new-schema
description: Создаёт новую бизнес-схему из описания на русском языке
---

# Создание новой схемы

Пользователь описывает бизнес-процесс: $ARGUMENTS

## Инструкции

1. Разбей описание на блоки (вход → процессы → выход)
2. Определи типы узлов из доступных категорий (input, process, output, amplifier, alert)
3. Подбери эмодзи и цвета из CLAUDE.md цветовой схемы
4. Добавь шаблон в файл `src/utils/templates.ts`:
   - Создай массив узлов с автоматической расстановкой (входы слева, процессы по центру, выходы справа)
   - Создай массив связей между логически связанными узлами
   - Добавь шаблон в массив SCHEMA_TEMPLATES
5. Для каждого узла используй формат:
```typescript
node('prefix-N', 'Название', '🎯', 'category', NODE_COLORS.color, x, y)
```
6. Расстановка: шаг 200px по X между узлами, 150px по Y для параллельных веток
7. Определи потенциальные узкие горлышки и отметь их category: 'alert'
8. Запусти `npm run build` для проверки
