# Правила для Claude

## Изоляция схемы
- Эта папка содержит ОДНУ конкретную схему
- НЕ ТРОГАЙ другие папки в schemas/
- Все изменения ТОЛЬКО через API: PUT /api/schemas/{id}
- НЕ СОЗДАВАЙ файлы за пределами этой папки

## Контекст
- schema.json — основной файл схемы
- history/ — автоматические снапшоты (не трогай вручную)
